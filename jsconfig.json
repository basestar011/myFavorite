{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "~/*": ["./*"],
      "@/*": ["./src/*"],
      // 해당 경로에 module이 없다면 /* 까지 잡아주고,
      "@views/*": ["./src/views/*"],
      "@data/*": ["./src/components/data/*"],
      "@table/*": ["./src/components/table/*"],
      // module이 있으면 /* 를 생략한다.
    }
  },
  "exclude": [
    "node_modules",
    "dist"
  ]
}